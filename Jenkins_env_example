pipeline {
    agent any
        stages {
        stage('Example') {

            steps {
               script  {
                    env.step_start_date= sh(returnStdout: true, script: 'date')
                    echo "${env.step_start_date}"
               }
            }
        }
    }
}
